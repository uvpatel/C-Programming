#include <stdio.h>

int main(){
    /*
    1) Unary Operator : Operator that work on single operand.
    e.g. : 
        ++,-- incremental and decremental operator.
        
    2) binary operator : Operators that work on two operands.
        e.g. : 
            +,-,*,/,//,%     Arithmetic Operator.
            <,<=,>,>=,==,!=  Rational Operator
            &&, || ,!        Logical Operator.
            &,|,<<,>>,~,^    Bitwise Operator.
            =,+=,-=,*=,/=,%= Assignment Operator
            ?:               Ternary Or Conditional Operator
    
     3) Ternary Operator : Operators that work on three operands.
    
     
    */
    return 0;
}