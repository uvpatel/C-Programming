// 7. stdbool.h
// Use: Adds support for bool type in C (C99 and later)

#include <stdbool.h>
#include <stdio.h>

int main() {
    bool isActive = true;
    if (isActive) {
        printf("It's active!\n");
    }
    return 0;
}
