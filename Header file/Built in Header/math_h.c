// 4. math.h
// Use: Math functions (sqrt(), pow(), ceil(), floor(), sin(), cos())


#include <math.h>
#include <stdio.h>

int main() {
    double num = 16.0;
    printf("Square root of %.0f is %.2f\n", num, sqrt(num));  // 4.00
    return 0;
}
