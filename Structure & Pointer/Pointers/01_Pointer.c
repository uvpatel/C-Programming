#include <stdio.h>

int main(){
    /*About pointer: 
    
    A variable which contains an address of any other variable.
    
    any other variable.
    Doesn’t contain value of variable, contain address !
    Derived datatype


    Syntax:
    Datatype *Pointername;

    int *p or int* p or int * p→ same in c
    
    
    Here datatype is datatype of variable whose address is carried by pointer
        */
    return 0;
}