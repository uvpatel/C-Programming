#include <stdio.h>

int main(){
    int i;
    while (i < 10)
    {
        printf("%d" , i);
        i++;
    }

    printf("The value of i is %d",i);

    // it throws a garbage value.

    
    return 0;
}