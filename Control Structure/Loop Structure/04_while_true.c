#include <stdio.h>

int main(){
    int i = 1;
    while (1)
    {
        printf("%d",i);
        i++;
    }
    printf("The value of i is %d",i);
    //Remember always the non Zero number work as a true and 0 as a false in if else.
    
    return 0;
}