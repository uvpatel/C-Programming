#include <stdio.h>
#include <math.h>
int main(){
    int iterations;
    for (int i = 0; i < iterations; i++)
    {
        
        
        float num,cube;
        printf("Enter the number plate: ");
        scanf("%f", &num);
        cube = pow(num,3);
        
        printf("The cube of the number is: %f\n", cube);
    }

    return 0;
}